<div class="memos">
<div id="list"></div>
  <% @memos.reverse_each do |memo| %>
    <div class="upper-memo">
      <%# <p class="memo-user">user name</p>  %>
      <p class="memo-date">日時:<%= memo.created_at %></p>
    </div>
    <div class="upper-memo">
      <p class="memo-content"><%= memo.memo %></p>
      <p class="memo-delete"><%= link_to 'メモ削除', memo_path(memo.id), method: :delete %></p> 
    </div> 
  <% end %>
</div>


------------

<%= form_with model: @memo, id: "form" do |f|%>
  <%= f.text_field :memo, id: "memo" %>
  <%= f.submit "入力", id: "submit" %>
<% end %>


<%# <p class="alert-font"><%= flash[:alert] ></p> %>
<%= link_to 'トップページ', '/' %>