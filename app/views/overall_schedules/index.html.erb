

<hr style="border:0;border-top:1px solid red;">

<div>
  <%= link_to 'イベント追加', new_event_path, class: "add-event" %>

  <%= link_to '戻る',  root_path %>
</div>
<hr style="border:0;border-top:1px solid red;">



<%= month_calendar events: @overall_schedules do |date, events| %>
  <%= date.day %>

  <% events.each do |event| %>
    <div>
      <%= event.title %>
    </div>
  <% end %>

<% end %>